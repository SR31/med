server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location /medcard/ {
    try_files $uri =404;
    add_header Access-Control-Allow-Origin *;
  }

  location = /healthz {
    return 200 'ok';
    add_header Content-Type text/plain;
  }
}
